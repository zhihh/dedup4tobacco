# demo_v0.0

## 需求：

一个文档查重系统，目前的demo系统，接受3个txt纯文本文档，文档1、文档2、文档3，然后针对这三个文档进行查重，并且返回重复的文本段、文本段所属的文档、文本段在各自文档中的位置。

## 思路：

假设大模型的上下文不足以支撑完整的文档，因此我们需要切片，切片的粒度是值得研究的一个点，目前的demo我们根据换行来切分。其次，我们将三个文档的所有切片进行聚类（使用深度学习方法），初步分类之后，同一类中来自不同文档的文本就有重复（抄袭）的可能。此外聚类之后，如果某一类包含的文本段大于模型上下文长度（目前demo简化为大于10个切片）我们就在这一类的所有文本段中再次聚类，更加细粒度的分类，如此重复这个过程。最后的结果是将三个文档的所有切片分成不超过10个切片的类别，然后逐次喂给大模型，让大模型进行进一步判别。

## 技术难点：

1. 如何用数据库存储
2. 如何存储切片的所属文档，在原文档中的位置信息（元数据）
3. 如何设计详细的prompt，让大模型清楚我们的目的，以返回我们想要的结果

## 要求：

请你根据以上的需求、思路和技术难点，帮我实现一个demo的ipynb文件，要求使用langchain工具链，例如lang-graph，大模型使用openai的api，其他技术栈你来给最优的选择。针对技术难点进行详细的推理分析，给出解决方案，并体现在demo中。
最后，帮我生成用来测试demo系统的数据集。







# demo_v1

### 1. LangChain 生态相关问题

#### 1.1 LangChain 框架利用率低

提示：从以下几个角度思考，如何把以下方法加入到 demo 中以提升 demo 性能，并使用对应的 LangChain 框架功能实现。

##### 🟧 **Query Construction（查询构建）**

从自然语言问题生成结构化查询语句，以访问不同类型的数据库：

* **Relational DBs（关系型数据库）**

  * 使用：Text-to-SQL（自然语言转SQL）或 PGVector
  * 目的：将自然语言转化为 SQL 查询语句

* **Graph DBs（图数据库）**

  * 使用：Text-to-Cypher（自然语言转Cypher语言）
  * 目的：将自然语言转化为图数据库查询语句

* **Vector DBs（向量数据库）**

  * 使用：Self-query retriever（自查询检索器）
  * 目的：自动生成元数据过滤条件以增强向量查询

---

##### 🟥 **Query Translation（查询翻译）**

对用户问题进行重写、分解或生成假设文档以提高检索效果：

* **Query Decomposition（问题分解）**

  * 方法：Multi-query, Step-back, RAG-Fusion
  * 作用：拆解或重述原始问题

* **Pseudo-documents（伪文档）**

  * 方法：HyDE（Hypothetical Document Embeddings）
  * 作用：生成假设文档以辅助理解和检索

---

##### 🟨 **Routing（路由）**

选择使用的数据库或嵌入方式以提高检索相关性：

* **Logical Routing（逻辑路由）**

  * 使用 LLM 来选择使用哪个数据库

* **Semantic Routing（语义路由）**

  * 对问题进行嵌入（embedding），根据相似度选择合适的 prompt 模板

---

##### 🟦 **Indexing（索引）**

构建高效、可扩展的知识检索结构：

* **Chunk Optimization（块优化）**

  * Semantic Splitter：按字符、段落、语义边界优化文本切块

* **Multi-representation Indexing（多表示索引）**

  * Dense X, Parent Document：将文档转化为多种结构，如摘要

* **Specialized Embeddings（专业嵌入）**

  * 使用 Fine-tuning、ColBERT 等高级嵌入模型

* **Hierarchical Indexing（分层索引）**

  * RAPTOR：构建文档摘要的层次树结构，用于多级检索

---

##### 🟩 **Retrieval（检索）**

从多个数据库或文档源中提取高相关信息：

* **Ranking（排序）**

  * 方法：Re-Rank, RankGPT, RAG-Fusion
  * 功能：按相关性排序/过滤文档

* **Refinement（优化）**

  * CRAG：根据相关性对结果进行压缩、重排

* **Active Retrieval（主动检索）**

  * 当检索结果不相关时，重新检索或从外部（如 Web）拉取新数据

---

##### 🟪 **Generation（生成）**

利用大模型生成最终答案，同时反馈优化检索过程：

* **Active Retrieval（主动生成检索）**

  * 使用生成质量来判断是否需要重写问题或重新检索

* **Self-RAG, RRR（再生成机制）**

  * 结合生成能力进行答案优化

---

#### 1.2 加入 LangSmith，我需要使用这个服务来监控开发



### 2. 数据库相关问题：

#### 2.1 现在有两个表 clusters 和 duplicate_results 表没有用上，此外其余两个表也只是存储了，没有后续利用。

提示：

1. 请结合整个系统的布局，思考现在的数据库设计是否合理，现有的几个表是否是充分必要的，例如第二次聚类的的结果是否也要存储到数据库中；
2. 由于目前切片、聚类具有一些不确定性，因此我需要每次运行创建一个新的数据库（可以命名加时间戳）；
3. 尽量多复用数据库

#### 2.2 chroma 向量数据库没有利用起来

提示：

1. 要利用起来chroma向量数据库存储向量的能力，以及其他功能（如果有聚类的话）

建议：仅供参考

- **集成 Chroma**：生成嵌入后，全部写入 Chroma。后续聚类、pairwise 检查、召回等均可用 Chroma 检索相似片段，提升效率和扩展性。
- **高效召回**：对噪声点或新增片段，利用 Chroma 的 ANN 检索快速找到高相似度候选，补充查重。
- **持久化与扩展**：Chroma 支持大规模数据和持久化，便于后续系统扩展

### 3. 我们需要全面提升demo系统的性能

#### 3.1 检查数据结构的定义是否充分必要，如有需要请完善

提示：

1. TextSegment 类中的 cluster_id 属性改成 str 类型，后续存储到数据库中也要改成对应类型。因此，对应的函数中需要修改聚类为"0", "1", ..., 第二次聚类为 "1_1", "1_2", ..., 第0号聚类永远留给噪声。

#### 3.2 文档分割需要进行优化，当前根据'\n'切片太简陋

提示：

1. 查询现有的的切片思路和技术，对文档进行多元化切片


#### 3.3 聚类算法太过简陋

提示：

1. 能否使用 embedding 模型进行聚类
2. 聚类这一部分是demo系统的核心，请尽可能思考优化

#### 3.4 请进一步优化 prompt，避免其产生幻觉


### 4. demo 系统迭代问题

提示：

1. 这一个版本我需要你将各个模块封装起来，封装在各个python文件中，而不是简单的做一个 jupyter notebook，最终我需要组成一个现代化的 python 项目
2. 我需要你尽可能将以上提到的问题、方法、思路 融合进这个demo系统中，对于不能融洽的方法，请将其做成可以选择的接口，便于用户选择对应的技术路线


### 5. 我需要你按照以下次序给我反馈

1. 首先，深入阅读现有demo系统的源码和以上的提示，给出你的详细的优化方案，并自己反思优化方案是否充分必要。
2. 根据优化方案，生成完整的无错误的项目结构，和对应的完整代码。
3. 根据以上所有的提示和生成反馈，给我生成一个详细的项目报告。要求，项目报告尽可能详尽并且描述技术时尽可能看起来“高大上”，并突出我们的创新点，核心目的就是为了包装我们这个项目，突出其价值。思路：先生成项目报告目录，再根据目录逐个生成详细的内容。




# 提示

请你根据demo_v1的提示词，请根据提示词，深度思考，并根据提示逐步生成，一次生成不了没关系，可以多次生成。此外，我并不需要你真的创建文件，我只需要你生成内容即可，例如 XXX.py，文件内容如下。

生成要求：

1. 要求生成内容充分、必要、可行、可扩展；
2. 代码需要有详细的中文注释；
3. 小步快跑，快速迭代。我们每次以最小测试单位生成，我测试之后给你反馈，然后再继续生成。
4. 首先生成目录结构，并生成创建这些目录及文件的脚本。



# 数据利用

首先预处理成领域特定的大规模训练语料库，

1. 通过构建对比学习样本对和困难负样本集来微调embedding模型，实现招投标领域的语义表示学习；
2. 训练专门的相似度判别模型；
3. 建立高质量的评估基准数据集用于模型性能验证；
4. 把领域的专业术语构建为招投标知识图谱和领域词典，为系统提供先验知识；

